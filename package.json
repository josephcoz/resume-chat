{
  "name": "resume-chat",
  "version": "0.1.0",
  "private": true,
  "description": "Recruiter-facing chat over a sanitized summary of Joe Cosby-Johnson's resume.",
  "scripts": {
    "lint": "node scripts/sanitize-check.js",
    "test": "node scripts/test-linkfetch.js && node scripts/test-markdown.js && node scripts/check-story-depth.js",
    "check": "npm run lint && npm run test && tsc --noEmit",
    "dev": "npm run lint && wrangler dev",
    "preview": "npm run check && wrangler versions upload",
    "deploy": "npm run check && wrangler deploy",
    "build": "npm run lint"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.0.0"
  }
}
