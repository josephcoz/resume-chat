name = "resume-chat"
compatibility_date = "2026-01-15"
pages_build_output_dir = "./public"

# Workers AI binding — gives the Pages Function access to env.AI.run().
# No API key needed; auth is via the binding.
[ai]
binding = "AI"
